{
    "name": "unicon/yaml",
    "version": "1.0",
    "description": "Validates YAML file using given class and, if matches, creates an object. Understands PhpDoc, converts values if necessary. Works recursively, so the YAML file may have complicated structure, and the given class may have properties of other classes.",
    "type": "library",
    "license": "GPL-2.0-only",
    "autoload": {
        "psr-4": {
            "Unicon\\Yaml\\": "src/"
        }
    },
    "authors": [
        {
            "name": "Sasha Bo"
        }
    ],
    "require": {
        "php": ">=8.1",
        "unicon/unicon": "^1.0",
        "symfony/yaml": ">=6.0"
    }
}
